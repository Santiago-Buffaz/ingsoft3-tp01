# Copiá este archivo a .env antes del primer arranque:
#   cp .env.example .env
DB_PASSWORD=cambiar-por-una-clave-local
FRONTEND_PORT=3000

# Solo se usa con docker-compose.registry.yml. Debe estar en minúsculas.
REGISTRY_USER=tu-usuario
